# Composition references

`nookii-home-clay-reference.jpg` is a development-only visual reference for the Phase 1 home-scene blockout.

It is never used as the rendered 3D background. Run the app with `?compare=1` to overlay the reference at low opacity while calibrating the actual React Three Fiber scene.
